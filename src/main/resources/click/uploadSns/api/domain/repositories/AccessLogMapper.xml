<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="click.uploadSns.api.domain.repositories.AccessLogMapper">

    <!-- resultmap書く -->

<insert id="insert" parameterType="click.uploadSns.api.domain.models.AccessLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO 
            upload_sns.access_log (
                access_uri,
                user_id,
                insert_time
            )
        VALUES (
            #{accessUri},
            #{userId},
            now()
        );
</insert>
</mapper> -->